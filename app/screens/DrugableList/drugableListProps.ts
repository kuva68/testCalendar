import type {NativeStackScreenProps} from '@react-navigation/native-stack';
import {RootParams} from '../../navigation/rootNavigator';

export type DrugableListScreenProps = NativeStackScreenProps<
  RootParams,
  'DrugableList'
>;
